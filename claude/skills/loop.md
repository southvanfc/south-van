# Skill: /loop

Runs a prompt or slash command on a recurring interval.

## When to use

- Polling a Vercel deploy after pushing to `main`
- Watching a long build for completion

## Example

```
/loop 2m check if the Vercel deploy has completed
```
